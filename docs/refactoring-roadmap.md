# Refactoring roadmap

This living roadmap tracks behavior-preserving, independently reviewable refactors—not feature work. Baseline: `6964594`.

## Status legend

- **Completed** — merged and recorded here.
- **Ready** — audited and bounded for implementation.
Items 24 and 35 are Ready. Items 1–23, 25–34, and 36 are Completed.

## Ranked work

| Rank / status | Ownership / files | Intent | Scope guardrails | Validation | Commit when completed |
| --- | --- | --- | --- | --- | --- |
| 1 · **Completed** | Application: `crates/application/src/issue_pagination.rs`, `crates/application/src/issue_pull.rs`, `crates/application/src/sync.rs`, `crates/application/src/lib.rs` | Share issue pagination, cursor safety, and response deduplication policy between pulls and sync. | Preserve first-seen ordering, latest snapshots, cancellation, cursor-cycle checks, page limits, and server-time behavior; keep application independent of adapters. | Boundary: focused application pagination, pull, and sync tests. | `b16b18e` |
| 2 · **Completed** | Application: `crates/application/src/comment_pagination.rs`, `crates/application/src/issue_detail.rs`, `crates/application/src/lib.rs` | Extract comment pagination state from issue-detail orchestration. | Preserve start-at/cursor handling, totals, limits, cycle/progress checks, and returned comment order; no port or persistence changes. | Boundary: focused comment-pagination and issue-detail tests. | `6f3eb55` |
| 3 · **Completed** | GPUI presentation: `apps/gpui/src/dashboard.rs` → existing `apps/gpui/src/presentation/updates.rs` (or a private sibling module) | Move pure update-feed policy out of the dashboard. | Move only `UpdateFilter`, `filtered_update_group_indices`, `CompactedUpdateRow`, `compact_update_rows`, `generic_summary_label`, preview-count helpers/constants, and `update_group_event_ids`. Keep rendering and handlers in `dashboard.rs`; preserve ordering, read/unread, and mark-read semantics. | Focused dashboard/presentation tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `6fcc86f` |
| 4 · **Completed** | Application test modules → `crates/application/src/test_support.rs` under `cfg(test)`; current helpers in `crates/application/src/comment.rs`, `crates/application/src/issue_detail.rs`, `crates/application/src/issue_edit.rs`, `crates/application/src/issue_media.rs`, `crates/application/src/issue_pull.rs`, and `crates/application/src/sync.rs` | Consolidate duplicated `block_on`/no-op-waker test helpers. | Test-only module and imports; no runtime dependency, fake/fixture consolidation, or production API. Keep each test’s behavior and ownership intact. | Focused application tests, then relevant application/workspace validation. | `a7a710f` |
| 5 · **Completed** | `crates/jira/src/jql.rs` only | Consolidate bounded Jira issue-ID validation shared by `enhanced_search_request_for_issue_ids` and `bulk_changelog_request`. | Extract the duplicated empty/1000 count, blank/255-byte/unsafe-character validation, lexicographic sort, and dedup pipeline. Check original count before dedup; preserve exact `JqlError` variants/order, output order, requests, JQL, page sizes, and tokens; do not merge account-ID rules. | Focused JQL tests, full jira-adapter tests, lib Clippy/fmt. | `92c4bc9` |
| 6 · **Completed** | Private storage helper plus `crates/storage/src/{lib.rs,memory.rs,sqlite.rs}` | Centralize update-event identity and association semantics across stores. | Identity remains exactly ID/site/issue ID/key/kind/occurred_at; merge only matching_user_set_ids; preserve payload/read/delivery state, ordering, atomic validation, and SQLite transaction boundaries; no SQL/schema/public API change. | Duplicate/conflict/rollback tests and full storage tests/Clippy. | `9c895a4` |
| 7 · **Completed** | `crates/application/src/sync.rs` only | Extract post-commit notification delivery from SyncService orchestration. | Commit completes before delivery; preserve inserted-event order, baseline suppression, mention precedence, policy once, dispatch once/no retries, best-effort records, counters, timestamps, non-fatal failures, and no post-commit cancellation check. | Focused sync and application tests, commit-before-delivery regression, lib Clippy/fmt. | `6964594` |
| 8 · **Completed** | GPUI presentation: `apps/gpui/src/dashboard.rs`, `apps/gpui/src/live_workspace.rs` | Extract typed scope/team settings transactions from Dashboard and model restored versus rollback-failed outcomes. | Preserve operation order, typed recovery branching, and no automatic retries; do not use message-substring matching. | Focused GPUI settings transaction, restore, and rollback-failure tests on a supported native host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `7827c4b` |
| 9 · **Completed** | GPUI presentation: `apps/gpui/src/dashboard.rs` | Separate comment-flow state from Dashboard orchestration. | Preserve consumable, one-dispatch confirmation; selection-change behavior; and `UnknownOutcome` reconciliation. Do not create a generic write flow. | Focused comment-flow tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `e769da9` |
| 10 · **Completed** | GPUI presentation: `apps/gpui/src/dashboard.rs` | Separate issue-edit state from Dashboard orchestration. | Preserve consumable, one-dispatch confirmation; selection-change behavior; and `UnknownOutcome` reconciliation. Keep this state machine distinct from comment flow. | Focused issue-edit-flow tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `2e55024` |
| 11 · **Completed** | GPUI presentation: `apps/gpui/src/dashboard.rs`, `apps/gpui/src/live_workspace.rs` | Add UI-owned cancellable request epochs and shared selected-detail/remote-lookup payload loading. | Epoch ownership stays in the UI; exclude dispatched Jira writes; preserve selection invalidation and stale-result suppression. | Focused selection/request-epoch tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `6dc1e81` |
| 12 · **Completed** | GPUI presentation: `apps/gpui/src/dashboard.rs`, `apps/gpui/src/dashboard/media.rs` | Extract media policy independently from diagnostics. | Preserve MIME/signature checks, byte/image caps, portal intent, create-new writes, cleanup, and fallback ordering; operational media acceptance must not depend on diagnostics enums; diagnostics may consume the shared classification policy. | Focused media-policy tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `9658092` |
| 13 · **Completed** | GPUI presentation: `apps/gpui/src/dashboard/media.rs`, `apps/gpui/src/live_workspace.rs` | Decompose media catalog, loader, and local-download responsibilities. | Preserve catalog ordering, bounded downloads, cleanup, create-new writes, portal intent, and existing fallback ordering. | Focused catalog/loader/download tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `ed9f013` |
| 14 · **Completed** | Jira adapter: `crates/jira/src/mapping.rs`, `crates/jira/src/lib.rs` | Extract Jira ADF/media parsing from ordinary issue mapping. | Preserve privacy filtering, URL allowlisting, bounds, placeholders, UTF-8 behavior, and the public facade. | Focused parser golden/property tests and full Jira adapter tests. | `368a0c5` |
| 15 · **Completed** | Jira HTTP: `crates/jira-http/src/lib.rs` | Decompose private Jira HTTP internals by read-response, attachment-response, exactly-once-write, and runtime bridge semantics only. | Keep item 31's Jira JSON codec move out of scope. Any shared bounded-body collector must return neutral failures mapped separately by read and write callers; never merge read/write retry or error policy. | Focused Jira HTTP transport, response, exactly-once-write, runtime-bridge, and error-policy tests plus Clippy/fmt. | `2e15689` |
| 16 · **Completed** | Storage: `crates/storage/src/{lib.rs,memory.rs,sqlite.rs}` and shared test support | Add shared storage-port conformance tests before storage decomposition. | Share contracts and validation for `InMemoryStore` and `SqliteStore`; do not share persistence mechanics or alter transaction behavior. | Conformance suite against both stores, then full storage tests/Clippy. | `c7d2911` |
| 17 · **Completed** | Storage: `crates/storage/src/sqlite.rs` | Decompose SQLite modules after storage conformance exists. | Preserve the single worker, transaction boundaries, locking, ordering, and error behavior; no decomposition before item 16. | Conformance suite plus focused and full SQLite tests. | `c54c7bd` |
| 18 · **Completed** | Application ports and Jira HTTP: `crates/application/src/ports.rs`, `crates/jira-http/src/lib.rs` | Split `JiraReadPort` into narrower capability ports incrementally. | Preserve unsupported defaults during migration and keep transport details out of application ports. | Port contract tests, adapter implementation tests, application tests, and Clippy/fmt. | `a957b51` |
| 19 · **Completed** | Application sync: `crates/application/src/sync.rs` and related models | Extract sync activity enrichment for changelog and mentions. | Retain commit/cursor ownership and the already-extracted post-commit notification order; do not move delivery semantics. | Focused enrichment, cursor, and sync tests. | `39e0862` |
| 20 · **Completed** | Application: `crates/application/src/issue_pull.rs`, `crates/application/src/sync.rs` | Consolidate remote issue-fetch scope validation and request construction for IssuePull and Sync. | Keep notification scope separate and do not duplicate the completed pagination work. | Focused scope/request tests for pull and sync. | `e905556` |
| 21 · **Completed** | Application-private identity module plus `crates/application/src/issue_diff.rs`, `crates/application/src/sync.rs`; storage validation coverage only | Centralize the duplicated versioned update-event identity implementation. | Keep snapshot/changelog event IDs and comment IDs in separate functions. Preserve all identity inputs and the existing big-endian length encoding for snapshot/changelog IDs versus little-endian length encoding for comment IDs; neither encoding may change. Storage validates the policy but does not own it. | Golden exact-ID tests for both identity paths, application event tests, and storage validation tests. | `36e969d` |
| 22 · **Completed** | Application cache/editing: `crates/application/src/issue_edit.rs` plus relevant application cache port/contracts (`crates/application/src/ports.rs`, `crates/application/src/model.rs` only if needed) | Encapsulate issue-edit cache freshness, local filtering, and typed invalidation policy. | Keep confirmed Jira writes in `IssueEditService`; preserve freshness and invalidation behavior. | Focused cache/filter/invalidation tests and issue-edit tests. | `5325a55` |
| 23 · **Completed** | Domain/application/Jira adapter: `crates/domain/src/issue_detail.rs`, `crates/application/src/issue_detail.rs`, `crates/jira/src/mapping.rs` | Make currently infallible `IssueDetailCore` and `IssueDetail` constructors honestly infallible. | Remove false failure paths, including the currently mapped impossible constructor error in `crates/application/src/issue_detail.rs`, unless a real invariant is introduced; preserve mapping and aggregate behavior. | Validation must cover domain, application, and Jira adapter tests. | `c20abfe` |
| 24 · **Ready** | GPUI presentation: `apps/gpui/src/live_workspace.rs`, `apps/gpui/src/dashboard.rs` | Model primary/team LiveWorkspace sync as explicit partition specs/builders. | Preserve team no-op, watcher differences, notification restriction, and cache isolation. | Focused workspace tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | — |
| 25 · **Completed** | GPUI diagnostics: `apps/gpui/src/diagnostics.rs` | Split diagnostics schema/serialization from bounded hardened file persistence. | Preserve sequencing, bounded writes, privacy, and `record_once` semantics. | Focused diagnostics schema and persistence tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `7afd5d0` |
| 26 · **Completed** | GPUI presentation: `apps/gpui/src/rich_text_view.rs` | Decompose rich-text rendering along block, inline, image, and policy boundaries. | Retain one shared render budget plus ordinal and accessibility behavior. | Focused rendering/policy tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `a6d7961` |
| 27 · **Completed** | Non-GPUI tests: large inline `#[cfg(test)]` modules under `crates/**/src` | Move large inline test modules to private sibling files before related semantic refactors. | Test ownership and visibility only; preserve fixtures, behavior, and production APIs. | Each affected crate’s focused tests, then relevant workspace tests. | `b0078cd` |
| 28 · **Completed** | GPUI tests: `apps/gpui/src/dashboard.rs` and private sibling test module | Move Dashboard’s large inline tests into a private sibling test module before further semantic refactors. | Mechanical move only; preserve fixtures, visibility, and test behavior before semantic refactors. | Focused Dashboard tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | `e5cfb37` |
| 29 · **Completed** | Jira adapter protocol: `crates/jira/src/lib.rs`, `crates/jira/src/mapping.rs` | Remove or deprecate obsolete `JiraSearchGateway` and its `RemoteIssue` projection protocol. | Require exhaustive reference audit; keep active domain mapper and transport models, and preserve active read behavior. | Reference audit, adapter tests, and public API/Clippy review. | `7fd84a7` |
| 30 · **Completed** | GPUI package surface: `apps/gpui/src/lib.rs` and exhaustive consumers | Reduce unused GPUI public re-exports after the protocol audit. | Preserve the binary-facing `AppShell`/startup bootstrap surface; remove only demonstrably unused surface and avoid compatibility changes beyond the audit. | Exhaustive workspace reference audit plus focused GPUI tests on a supported native host, with Linux Wayland or macOS smoke where behavior is platform-sensitive. | `2cf7c2c` |
| 31 · **Completed** | Jira adapter/HTTP boundary: `crates/jira/src/{models.rs,lib.rs}`, `crates/jira-http/src/lib.rs` | Move Jira JSON codecs for transition responses and explicit write bodies into the pure Jira adapter. | Keep dispatch, exactly-once behavior, and transport/runtime policy in `jira-http`. | Codec golden tests plus focused Jira HTTP write tests. | `d3bdcb5` |
| 32 · **Completed** | Jira HTTP reads: `crates/jira-http/src/lib.rs` | Centralize token-page progress guards for HTTP-owned bulk reads. | Preserve endpoint-specific current behavior first; any hardening behavior change is a separate roadmap item. | Focused page-progress/cycle tests for each bulk-read endpoint. | `9141aff` |
| 33 · **Completed** | Desktop notifications: `crates/desktop-notifications/src/lib.rs` | Add a private backend seam for deterministic one-call, failure, and retention tests. | Preserve `NotifyRust` handle lifetime, one dispatch, and no retries; keep the seam private. | Focused deterministic notification tests. | `86d12a7` |
| 34 · **Completed** | Workspace docs/manifests/tests: `README.md`, `Cargo.toml`, package manifests, brittle count snapshots | Correct documentation and manifest drift for remembered credentials, workspace `futures-lite` inheritance, and hard-coded test-count snapshots. | Documentation/configuration/test-maintenance only; do not change runtime behavior or broaden snapshot policy. | Targeted documentation/config checks and affected tests. | `c4ad942` |
| 35 · **Ready** | Pure Dashboard presentation policy: `apps/gpui/src/dashboard.rs` and presentation modules | Replace tuple/operation-string Dashboard outcome/error copies with typed presentation policy after feature-state seams. | Do not duplicate completed update-feed roadmap item 3; preserve user-facing wording and outcome mapping. | Focused presentation tests on a supported native GPUI host, plus Linux Wayland or macOS smoke where behavior is platform-sensitive. | — |
| 36 · **Completed** | Application notification tests: `crates/application/src/notifications.rs` test code | Mechanically remove current test-lint `unwrap` violations. | Test-lint hygiene only; no broad pedantic cleanup and no production behavior change. | Targeted application all-target Clippy for `crates/application/src/notifications.rs` test code. | `624f9fd` |

Platform-sensitive GPUI validation uses a supported native host and the relevant Linux Wayland or macOS smoke; unrelated all-target Clippy blockers remain context only. Item 36 is a narrow Completed test-lint cleanup and does not broaden that context.

## Workflow

Select the highest-ranked **Ready** item, assign one Luna writer with bounded file ownership, preserve behavior, and run focused plus relevant broad validation. Obtain independent review. Sol commits one coherent change, then updates this roadmap in a separate documentation commit when the status changes.
